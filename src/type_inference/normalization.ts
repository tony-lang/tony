import { ResolvedConstrainedType } from '../types/type_inference/constraints'

/**
 * Given a type, reduce the type until it is normal.
 */
export const normalize = (
  type: ResolvedConstrainedType,
): ResolvedConstrainedType => {}
